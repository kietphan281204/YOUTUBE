// Frontend runtime config (safe to commit).
// When hosting on GitHub Pages, set this to your deployed backend URL.
// Example: window.API_BASE = "https://proceed-pensions-stranger-george.trycloudflare.com";
//
// When running locally via `node server.js` and opening http://localhost:<PORT>,
// you can leave it empty to use same-origin requests.
// Local mode: để rỗng để gọi cùng origin (http://localhost:8080).
// Khi deploy GitHub Pages, đổi thành URL ngrok/backend hiện tại.
// URL ngrok hiện tại (mỗi lần ngrok đổi link thì cập nhật lại + push).
window.API_BASE = "https://proceed-pensions-stranger-george.trycloudflare.com";
